# 安全策略

## 支持范围

当前仅维护最新发布版本和默认分支。旧版本问题可能要求先升级后复现。

## 私密报告

请优先使用 GitHub 仓库的 **Private vulnerability reporting**。不要在公开 Issue、Discussion、截图或日志附件中披露漏洞细节、Cookie、会话令牌或个人数据。

如果仓库尚未启用私密漏洞报告，请先通过维护者公开资料建立联系，只发送“需要私密报告安全问题”的请求；确认私密渠道后再提供技术细节。

报告应包含：

- 受影响版本
- 前置条件和复现步骤
- 输入点到敏感操作的完整路径
- 实际影响
- 建议缓解方向
- 是否已公开或计划公开

## 数据与隐私

YAM 本地保存数据库、Cookie、设置和诊断日志，不提供自动上传。报告问题时请勿提交：

- `~/.yam/cookies/`
- `~/.yam/data/*.db`
- 未脱敏的 `~/.yam/logs/`
- 导出中可能包含的个人备注或自定义内容

项目不会要求提供研招网密码或完整 Cookie。

## 调试端口

CDP 9223 仅用于 `npm run desktop:dev`。若发布包监听该端口，请按高优先级安全问题私密报告。
