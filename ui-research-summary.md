# YAM UI 设计研究总结

## 1. 收集案例概览

本次研究共收集 **10 个 UI 设计案例**，覆盖桌面 Dashboard、教育信息聚合、数据查询/筛选/对比、卡片式列表+详情面板、空状态、表单筛选等方向。每个案例均包含一张截图、来源 URL 与一句话参考点，存放于 `cases/` 目录。

| 编号 | 案例 | 主要参考方向 |
|------|------|--------------|
| 01 | Behance 选校相关搜索页 | 教育类信息聚合、卡片列表 |
| 02 | Behance Busla 校园交通管理系统 | 详情页顶部信息架构、配色 |
| 03 | GitHub 学校管理后台首页 | Dashboard、KPI 卡片、左侧导航 |
| 04 | GitHub 学校管理后台学生列表 | 表格视图、搜索、操作列 |
| 05 | Tabler Admin Dashboard | 现代后台首页、KPI 卡片、视觉风格 |
| 06 | Tabler 垂直侧边栏 | 左侧导航结构、可折叠菜单 |
| 07 | Visme 对比图表模板 | 2-3 项并排对比、差异高亮 |
| 08 | Microsoft List/details 模式 | 列表-详情分栏布局 |
| 09 | Ant Design Empty 空状态 | 空状态/数据缺失提示 |
| 10 | Element Plus Form | 筛选/表单控件布局 |

## 2. 出现频率最高的 3 个设计模式

### 模式 1：左侧垂直导航 + 顶部标题/面包屑 + 主内容区
教育类后台（案例 03）与 SaaS Dashboard（案例 05、06）均采用这一经典桌面布局：深色或浅色左侧边栏承载一级导航，顶部显示面包屑、页面标题与全局操作按钮，主内容区放卡片、表格或图表。该模式适合 YAM 作为本地桌面应用固定窗口布局，而非响应式网页。

### 模式 2：卡片式信息容器
从 Behance 项目网格（案例 01）到 Tabler 的 KPI 统计卡片（案例 05），卡片是承载离散信息的核心单元。典型结构为：标题/名称 + 关键元信息 + 操作入口（收藏、查看、编辑）。YAM 的院校列表、首页统计、详情页数据分组均可采用卡片化表达。

### 模式 3：列表-详情分栏（List/Details）
Microsoft 官方模式（案例 08）与 GitHub 学生管理（案例 04）都体现「左侧列表选中，右侧展示详情」的交互。院校列表与详情页天然适合此模式：左侧可搜索/筛选，右侧固定显示选中院校详情，避免页面跳转。

## 3. 对 YAM 各页面的具体建议

### 首页
- 采用「三段式」：左侧导航（首页 / 院校列表 / 对比 / 收藏 / 设置）+ 顶部标题区 + 主内容区。
- 顶部放置 3-4 个 KPI 统计卡片：关注院校数、已对比数、数据覆盖年份、最近更新日期。
- 下方分两块：「最近查看」用横向卡片滚动或栅格；「数据概览」用简单图表或趋势箭头展示院校/专业数量分布。
- 参考：案例 03、05。

### 院校列表页
- 默认采用卡片网格视图，支持一键切换到表格视图（案例 01、04）。
- 顶部工具栏顺序：搜索框 → 筛选条件（地区、门类、年份等）→ 排序下拉 → 视图切换图标 → 导出/收藏操作。
- 卡片内容：院校名称、所在地、层次（985/211/双一流）、关键分数线/招生人数、收藏按钮。
- 表格视图注重行高、对齐、状态标签与操作列（查看 / 对比 / 收藏）。
- 参考：案例 01、04、10。

### 院校详情页
- 顶部信息区：大标题 + 地点/层次标签 + 关键元数据（代码、隶属、官网），参考案例 02。
- 下方用 Tab 分组：「基本信息」「历年分数线」「招生计划」「专业目录」「备注/异常」。
- 每个 Tab 内使用数据卡片承载不同维度信息，关键数字突出显示。
- 数据缺失处使用空状态组件，明确提示「暂无 2024 年数据」并给出返回或反馈入口，参考案例 09。
- 参考：案例 02、08、09。

### 对比页
- 采用「属性行 × 院校列」的表格布局，支持 2-3 所院校并排对比。
- 关键差异字段（如分数线、招生人数）用背景色或箭头高亮差异。
- 顶部提供「添加院校」和「移除院校」操作，空位显示占位卡片引导用户补充。
- 参考：案例 07。

### 收藏 / 关注列表
- 可作为院校列表的一个预置筛选视图，复用列表页的卡片/表格组件。
- 在每条收藏项上突出「取消关注」操作，并支持批量管理（全选、取消收藏）。
- 空状态使用统一插画+文案：「暂无关注院校，去列表页添加」。
- 参考：案例 01、04、09。

## 4. 推荐的整体视觉方向

### 颜色
- 主色：清爽的蓝白基调（参考 Tabler、GitHub 学校后台），传达理性、可信赖的教育工具感。
- 辅助色：低饱和的暖黄/浅绿用于状态标签、趋势上升；红色用于异常/缺失提示。
- 背景：浅灰（#F5F7FA 附近）内容区 + 白色卡片，形成明确层级。

### 布局
- 固定左侧导航栏（宽度 220-260px），主内容区留足呼吸感。
- 卡片使用 8-12px 圆角、1px 浅灰边框或极淡阴影，避免过度装饰。
- 列表-详情页采用固定比例分栏（如 40% / 60%），可拖拽调整宽度。

### 密度
- 中密度：行高与卡片间距略大于网页后台，适配桌面端长时间浏览。
- 关键数字放大、标签/辅助信息缩小并置灰，形成清晰的信息层级。
- 按钮与控件尺寸适中（32-40px 高），保证鼠标操作舒适度。

---

*研究完成时间：2026-07-07*  
*案例目录：`d:\考研信息整理\.trae\documents\ui-references\cases\`*
